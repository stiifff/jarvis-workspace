# Lo MÍNIMO que el motor necesita para arrancar y trabajar.
#
# Es lo que viaja adentro del instalador. Se deriva de plotspace/requirements.txt
# sacando los extras pesados:
#
#   playwright + chromium ...... ~780 MB   solo el browser remoto del Preview
#   onnx-asr / onnxruntime ..... ~400 MB   dictado local (con Groq no hace falta)
#   faster-whisper ............. ~60 MB    dictado local, vía de escape
#   numpy / Pillow ............. ~50 MB    solo los usan los anteriores
#
# Con todo eso adentro el instalador pasa de ~150 MB a ~1,3 GB, y la mayoría de
# la gente no usa ninguna de esas funciones. Se instalan aparte cuando el
# usuario las prende.
#
# Si el motor arranca acá pero falla en producción por un import que falta, el
# arreglo es agregarlo a esta lista — no volver a meter requirements.txt entero.
fastapi>=0.104.0
uvicorn>=0.24.0
websockets>=12.0
httptools>=0.6.0
# Solo Unix: arrastra pty/termios/fcntl. En Windows el motor de
# terminales es el termhost y este paquete no hace falta.
ptyprocess>=0.7.0; sys_platform != "win32"
psutil>=5.9.0
aiofiles>=23.0.0
python-multipart>=0.0.6
anthropic>=0.100.0
httpx>=0.27.0
python-dotenv>=1.0.0
edge-tts>=6.0.0
